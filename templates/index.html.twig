<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Appel du CSS compilé via Webpack Encore avec versionnement pour éviter la mise en cache -->
    {{ encore_entry_link_tags('app') }}
    {{ encore_entry_link_tags('styles/app_css') }}
    <link rel="stylesheet" href="{{ asset('build/styles/app_css.css') }}?v={{ random() }}">

    <title>Gamestore : magasins de jeux vidéos</title>
</head>

<body>
    <header id="navbar-container">
        <!-- Wrapper pour le logo hors de la zone clip-path -->
        <div class="logo-container">
            <img src="{{ asset('build/images/logoGamestore.png') }}" alt="Gamestore logo" class="logo">
        </div>
        <nav class="navbar navbar-expand-lg bg-custom">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_homepage') }}">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('galerie') }}">Nos jeux vidéos</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#"><i class="bi bi-bag-heart"></i></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#"><i class="bi bi-person-circle"></i></a>
                    </li>
                </ul>
            </div>
        </nav>
    </header>

    <!-- CAROUSEL Gamestore -->
    <section id="carousel-section">
        <div id="monCarousel" class="carousel slide carousel-fade" data-bs-ride="carousel">
            <div class="carousel-indicators">
                <button type="button" data-bs-target="#monCarousel" data-bs-slide-to="0" class="active"
                    aria-current="true" aria-label="Slide 1"></button>
                <button type="button" data-bs-target="#monCarousel" data-bs-slide-to="1" aria-label="Slide 2"></button>
                <button type="button" data-bs-target="#monCarousel" data-bs-slide-to="2" aria-label="Slide 3"></button>
            </div>

            <div class="carousel-inner">
                <div class="carousel-item active">
                    <img src="{{ asset('build/images/fps/fpsGameplay.png') }}" class="d-block w-100" alt="FPS Gameplay">
                    <div class="carousel-caption d-none d-md-block">
                        <h5>Nouveauté</h5>
                        <p>Nouveau FPS disponible.</p>
                    </div>
                </div>

                <div class="carousel-item">
                    <img src="{{ asset('build/images/platform/plateFormCine.png') }}" class="d-block w-100"
                        alt="PlateForm Cine">
                    <div class="carousel-caption d-none d-md-block">
                        <h5>Promotion du mois</h5>
                        <p>- 20 % sur nos RPG.</p>
                    </div>
                </div>

                <div class="carousel-item">
                    <img src="{{ asset('build/images/rpg/rpgCine.png') }}" class="d-block w-100" alt="RPG Cine">
                    <div class="carousel-caption d-none d-md-block">
                        <h5>Découvrez tous nos jeux</h5>
                        <p>Jeux disponibles pour tous les goûts.</p>
                    </div>
                </div>
            </div>

            <button class="carousel-control-prev" type="button" data-bs-target="#monCarousel" data-bs-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Previous</span>
            </button>

            <button class="carousel-control-next" type="button" data-bs-target="#monCarousel" data-bs-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Next</span>
            </button>
        </div>
    </section>

    <!-- SECTION PRÉSENTATION -->
    <section id="presentation">
        <div class="presentation-box">
            <h2>Gamestore, bien plus qu'un magasin !</h2>
            <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Turpis lacus turpis fringilla et.</p>
        </div>
    </section>

    <!-- FOOTER -->
    <footer>
        <p>&copy; 2024 Gamestore - Tous droits réservés</p>
        <p><a href="#">Mentions légales</a> | <a href="#">Politique de confidentialité</a></p>
    </footer>

    <!-- Appel du JavaScript compilé via Webpack Encore -->
    {{ encore_entry_script_tags('app') }}

    <!-- Ajout du fichier JavaScript de Bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>